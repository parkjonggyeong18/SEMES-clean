<Page x:Class="semes.Pages.UserManagementPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d"
      Title="UserManagementPage">

    <Grid>
        <materialDesign:Card Padding="30" Background="White" Margin="10">
            <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="10"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="회원관리" FontSize="20" FontWeight="Bold" Margin="10 10 0 20"/>

                    <!-- 입력 영역 -->
                    <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="20 0 0 10" >
                        <TextBox x:Name="txtUserId"
                                 Width="200"
                                 Margin="0 0 10 0"
                                 Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                                 materialDesign:HintAssist.Hint="아이디"/>

                        <PasswordBox x:Name="txtPassword"
                                     Width="200"
                                     Margin="10 0 80 0"
                                     Style="{StaticResource MaterialDesignFloatingHintPasswordBox}"
                                     materialDesign:HintAssist.Hint="비밀번호"/>

                        <Button Content="회원 추가"
                                Width="100"
                                Click="AddUser_Click"
                                Style="{StaticResource MaterialDesignOutlinedButton}" />
                    </StackPanel>

                    <!-- 회원 목록 -->
                    <ListView x:Name="userListView" Grid.Row="3" Margin="10 10 0 0">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="Margin" Value="0"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="VerticalContentAlignment" Value="Top"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListViewItem">
                                            <Border x:Name="Border"
                                Background="Transparent">
                                                <Grid>
                                                    <ContentPresenter Margin="5 7"/>

                                                    <Border Height="1" Background="#DDD" VerticalAlignment="Bottom"/>
                                                </Grid>
                                            </Border>

                                            <!-- 호버 및 선택 효과 -->
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Border" Property="Background" Value="#f5f5f5"/>
                                                </Trigger>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="Border" Property="Background" Value="#e0e0e0"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListView.ItemContainerStyle>

                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <DockPanel LastChildFill="False" Margin="10 5">
                                    <TextBlock Text="{Binding Username}" Width="200" VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding Password}" Width="200" VerticalAlignment="Center" Margin="20 0 80 0"/>
                                    <Button Content="삭제" Tag="{Binding}" Click="DeleteUser_Click"
                                        Width="60" Style="{StaticResource MaterialDesignOutlinedButton}" />
                                </DockPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>

                </Grid>
            </materialDesign:Card>
        </Grid>
</Page>

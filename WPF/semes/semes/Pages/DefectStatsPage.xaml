<Page x:Class="semes.DefectStatsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:semes"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
      mc:Ignorable="d" 
      d:DesignHeight="650" d:DesignWidth="1050"
      Title="DefectStatsPage">

    <Page.Resources>
        <LinearGradientBrush x:Key="MainBackground" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#E3F2FD" Offset="0"/>
            <GradientStop Color="#F5F5F5" Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="HeaderGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#1976D2" Offset="0"/>
            <GradientStop Color="#2196F3" Offset="1"/>
        </LinearGradientBrush>

        <Style x:Key="TechCardStyle" TargetType="materialDesign:Card">
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Background" Value="White"/>
        </Style>
    </Page.Resources>

    <Grid Background="{StaticResource MainBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 헤더 영역 -->
        <Border Grid.Row="0" Background="{StaticResource HeaderGradient}" Padding="30,20">
            <StackPanel Orientation="Horizontal">
                <!-- CPU 칩 아이콘 -->
                <Border Background="#FFFFFF" 
            CornerRadius="50" 
            Width="50" 
            Height="50" 
            Margin="0,0,20,0">
                    <Grid>
                        <Rectangle Width="30" Height="30" Fill="#1976D2" RadiusX="3" RadiusY="3"/>
                        <Rectangle Width="18" Height="18" Fill="White" RadiusX="2" RadiusY="2"/>
                        <Rectangle Width="6" Height="2" Fill="#1976D2" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,0,0,0"/>
                        <Rectangle Width="6" Height="2" Fill="#1976D2" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <Rectangle Width="2" Height="6" Fill="#1976D2" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,10,0,0"/>
                        <Rectangle Width="2" Height="6" Fill="#1976D2" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,10"/>
                    </Grid>
                </Border>

                <StackPanel VerticalAlignment="Center">
                    <TextBlock FontSize="28" 
                   FontWeight="Bold" 
                   Foreground="White" 
                   Text="불량률 현황">
                        <TextBlock.Effect>
                            <DropShadowEffect Color="#1976D2" BlurRadius="10" ShadowDepth="2" Opacity="0.5"/>
                        </TextBlock.Effect>
                    </TextBlock>
                    <TextBlock FontSize="14" 
                   Foreground="#E3F2FD" 
                   Text="SEMES 아산 사업장" 
                   Margin="0,5,0,0"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- 본문 영역 -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="300"/>
            </Grid.ColumnDefinitions>

            <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <materialDesign:Card Style="{StaticResource TechCardStyle}">
                        <StackPanel>
                            <TextBlock Text="불량률" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>
                            <lvc:CartesianChart Series="{Binding DefectRateCollection}" DisableAnimations="False" Height="200">
                                <lvc:CartesianChart.AxisY>
                                    <lvc:Axis Title="불량률 (%)" LabelFormatter="{Binding YFormatter}" MinValue="0"/>
                                </lvc:CartesianChart.AxisY>
                                <lvc:CartesianChart.AxisX>
                                    <lvc:Axis Labels="{Binding DateLabels}"/>
                                </lvc:CartesianChart.AxisX>
                                <lvc:CartesianChart.DataTooltip>
                                    <lvc:DefaultTooltip SelectionMode="SharedXValues"/>
                                </lvc:CartesianChart.DataTooltip>
                            </lvc:CartesianChart>
                        </StackPanel>
                    </materialDesign:Card>

                    <materialDesign:Card Style="{StaticResource TechCardStyle}">
                        <StackPanel>
                            <TextBlock Text="불량 수" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>
                            <lvc:CartesianChart Series="{Binding DefectCountCollection}" DisableAnimations="False" Height="200">
                                <lvc:CartesianChart.AxisY>
                                    <lvc:Axis Title="불량 수 (개)" MinValue="0"/>
                                </lvc:CartesianChart.AxisY>
                                <lvc:CartesianChart.AxisX>
                                    <lvc:Axis Labels="{Binding DateLabels}"/>
                                </lvc:CartesianChart.AxisX>
                            </lvc:CartesianChart>
                        </StackPanel>
                    </materialDesign:Card>

                    <materialDesign:Card Style="{StaticResource TechCardStyle}">
                        <StackPanel>
                            <TextBlock Text="불량 위치" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>
                            <Border BorderBrush="#DDD" BorderThickness="1">
                                <Grid x:Name="DefectPositionGrid" Height="200"/>
                            </Border>
                        </StackPanel>
                    </materialDesign:Card>
                </StackPanel>
            </ScrollViewer>

            <materialDesign:Card Grid.Column="1" Style="{StaticResource TechCardStyle}">
                <StackPanel>
                    <TextBlock Text="검사 이력" FontSize="20" FontWeight="Bold" Margin="0,0,0,16"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="검사 수:" Grid.Row="0" Grid.Column="0" Margin="0,0,10,8"/>
                        <TextBlock x:Name="검사수Text" Grid.Row="0" Grid.Column="1" FontWeight="SemiBold"/>

                        <TextBlock Text="양품 수:" Grid.Row="1" Grid.Column="0" Margin="0,0,10,8"/>
                        <TextBlock x:Name="양품수Text" Grid.Row="1" Grid.Column="1" FontWeight="SemiBold"/>

                        <TextBlock Text="불량 수:" Grid.Row="2" Grid.Column="0" Margin="0,0,10,8"/>
                        <TextBlock x:Name="불량수Text" Grid.Row="2" Grid.Column="1" FontWeight="SemiBold"/>
                    </Grid>
                    <TextBlock Text="날짜 선택" FontSize="14" FontWeight="Bold" Margin="0,16,0,8"/>
                    <Calendar x:Name="DefectCalendar" SelectedDatesChanged="DefectCalendar_SelectedDatesChanged" Language="ko"/>
                </StackPanel>
            </materialDesign:Card>
        </Grid>
    </Grid>
</Page>

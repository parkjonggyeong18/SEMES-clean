<Page x:Class="semes.Pages.CommunityPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:pages="clr-namespace:semes.Pages"
      xmlns:converters="clr-namespace:semes.Converters"
      xmlns:app="clr-namespace:semes"
      mc:Ignorable="d"
      Title="CommunityPage">

    <Page.Resources>
        <converters:AuthorVisibilityConverter x:Key="AuthorVisibilityConverter"/>
    </Page.Resources>


    <Grid>
        <materialDesign:Card Padding="30" Background="White" Margin="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="10"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <TextBlock Text="게시판" FontSize="20" FontWeight="Bold" Margin="10 10 0 20"/>

                <!-- 글쓰기 버튼 -->
                    <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0 30 10 10" HorizontalAlignment="Right">
                        <Button Content="글쓰기"
                                Width="80"
                                Click="WritePost_Click"
                                Style="{StaticResource MaterialDesignOutlinedButton}"/>
                    </StackPanel>

                <!-- 게시글 목록 -->
                <ListView x:Name="postListView" Grid.Row="2"
                          SelectionChanged="postListView_SelectionChanged" Margin="10 10 10 0">
                    <!-- 구분선 & Hover 효과 스타일 추가 -->
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Margin" Value="0"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="VerticalContentAlignment" Value="Top"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListViewItem">
                                        <Border x:Name="Border" Background="Transparent">
                                            <Grid>
                                                <ContentPresenter Margin="5 7"/>
                                                <Border Height="1" Background="{DynamicResource MaterialDesignDivider}" VerticalAlignment="Bottom"/>
                                            </Grid>
                                        </Border>

                                        <!-- Hover 및 선택 효과 -->
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Border" Property="Background" Value="#f5f5f5"/>
                                            </Trigger>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="Border" Property="Background" Value="#e0e0e0"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListView.ItemContainerStyle>

                    <!-- 게시글 항목 템플릿 -->
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <DockPanel LastChildFill="False" Margin="10 10">
                                <TextBlock Text="{Binding Title}" Width="150" VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding Content}" Width="300" Margin="20 0" VerticalAlignment="Center"/>
                            </DockPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
            </materialDesign:Card>
    </Grid>
</Page>